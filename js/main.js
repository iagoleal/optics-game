// Generated by CoffeeScript 1.7.1
(function() {
  var canvas, context, mainLoop;

  (function() {
    function _Class() {}

    return _Class;

  })();

  canvas = null;

  context = null;

  window.ang = null;

  window.onload = function() {
    canvas = document.getElementById("board");
    context = canvas.getContext("2d");
    window.ang = new Object({
      x: 400,
      y: 300
    }, 30);
    return requestAnimationFrame(mainLoop);
  };

  mainLoop = function() {
    canvas.width = canvas.width;
    ang.draw(context);
    ang.turn(1);
    return requestAnimationFrame(mainLoop);
  };

  window.requestAnimationFrame = (function() {
    return window.requestAnimationFrame || window.webkitrequestAnimationFrame || window.mozrequestAnimationFrame || function(cback) {
      return window.setTimeout(cback, 1000 / 60);
    };
  })();

}).call(this);
