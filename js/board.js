// Generated by CoffeeScript 1.6.3
(function() {
  var LaserGun, Mirror, Turnable, _ref, _ref1,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Turnable = (function() {
    Turnable.prototype.position = null;

    Turnable.prototype.angle = 0;

    function Turnable(position, angle) {
      this.position = position != null ? position : {
        x: 0,
        y: 0
      };
      this.angle = angle != null ? angle : 0;
    }

    Turnable.prototype.turn = function(dgr) {
      this.angle += dgr;
      if (this.angle > 360) {
        return this.angle -= 360;
      } else if (this.angle < 0) {
        return this.angle += 360;
      }
    };

    Turnable.prototype.draw = function(context) {
      return drawer.rectangle(context, "fill", this.angle, this.position, 100, 100);
    };

    return Turnable;

  })();

  Mirror = (function(_super) {
    __extends(Mirror, _super);

    function Mirror() {
      _ref = Mirror.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Mirror.prototype.img = null;

    Mirror.prototype.draw = function(context) {
      return drawer.rectangle(context, "fill", this.angle, this.position, 100, 100);
    };

    return Mirror;

  })(Turnable);

  LaserGun = (function(_super) {
    __extends(LaserGun, _super);

    function LaserGun() {
      _ref1 = LaserGun.__super__.constructor.apply(this, arguments);
      return _ref1;
    }

    LaserGun.prototype.img = null;

    LaserGun.prototype.draw = function(context) {
      return drawer.polygon(context, "fill", this.angle, this.position, 3, 30);
    };

    return LaserGun;

  })(Turnable);

  window.Mirror = Mirror;

  window.LaserGun = LaserGun;

}).call(this);
